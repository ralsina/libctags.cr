name: libctags
version: 0.1.0

authors:
  - Roberto Alsina <roberto.alsina@gmail.com>

targets:
  readtags:
    main: src/main.cr

dependencies:
  docopt:
    github: chenkovsky/docopt.cr

scripts:
    postinstall: cd ext && make

crystal: '>= 1.15.1'

license: MIT
